// Single source of truth for the fixed priority list on the frontend.
// Mirrors backend/constants.py — keep both in sync if the list ever changes.
export const PRIORITIES = ['Low', 'Medium', 'High', 'Critical'];