# Intelligent Helpdesk — Landing Page

Landing page only. No backend, auth, AI, or routing beyond the three role
stubs (`/employee`, `/resolver`, `/review`).

## Setup

```bash
npm install
npm run dev
```

Then open the local URL Vite prints (usually http://localhost:5173).

## Build

```bash
npm run build
npm run preview
```
